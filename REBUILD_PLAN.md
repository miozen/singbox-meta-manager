# 新项目分阶段改造计划

## 0. 当前进度

- 总计划：11 个阶段（含准备阶段 `0`，实施阶段 `1` 至 `10`）。
- 已完成：阶段 `0`、阶段 `1`、阶段 `2`、阶段 `3`、阶段 `4`、阶段 `5`、阶段 `6`、阶段 `7`、阶段 `8`、阶段 `9`、阶段 `10`。
- 当前进度：`11 / 11`。
- 当前所在：阶段 `10` 已完成，源码改造、发布前静态核对和远端验证清单已经收口。
- 下一步：同步源码到真实 Git 仓库并推送，由 GitHub + Cloudflare Worker 执行构建部署和线上验证。

## 1. 目标

基于当前 `singbox-meta-manager` 项目进行持续改造，最终形成一个部署在 Cloudflare Worker 上的新项目，并满足以下目标：

- 保留 `singbox-meta-manager` 现有模板管理完整能力。
- 管理端登录页与后台整体风格采用 `proxyhub` 的实现方向。
- 引入 `singbox-center` / `proxyhub` 的订阅源管理、生成测试、配置生成、失败回退等核心业务能力。
- 新项目的核心发布模型从“模板直出”升级为“客户端链接可绑定不同模板和不同订阅集合”。
- 不引入 `Sub-Store` 业务依赖。
- 对确定迁移的业务功能不做缩减。

## 2. 当前约束

- 当前在线部署由 Cloudflare Worker 跟踪 GitHub `master` 分支。
- 当前没有 `staging` / `preview` 环境。
- 本地开发阶段不进行本地运行测试。
- 阶段完成后再推送 GitHub，由 Worker 构建部署后进行在线验证。
- 当前工作目录为源码目录，不是已克隆的 Git 工作树；因此本地先按源码目录持续修改与留档，后续如需直接推送，需要切换到真实 Git 仓库目录或补齐 Git 元数据。

## 3. 建议执行方式

### 推荐方式

在没有独立测试环境、且明确“不做本地测试”的前提下，采用“单阶段完整交付，再推送部署”的方式最合适。

执行原则如下：

- 每次只做一个阶段，不跨阶段混改。
- 每个阶段内部允许分多次本地修改，但未完成前不推送。
- 每个阶段完成后，再统一同步到 GitHub `master`。
- Cloudflare Worker 自动构建部署后，再进行线上验证。
- 若阶段跨度较大、担心中途改造过半，可以临时使用 `dev` 分支承接该阶段，待阶段完成后再合并到 `master`。

### 分支建议

由于当前没有独立测试环境，单纯建立 `dev` 分支并不能直接带来线上测试价值，因此推荐如下：

- 默认策略：按阶段直接改造并同步到 `master`。
- 例外策略：当某个阶段改动面特别大、无法在一次连续工作中完成时，可临时使用 `dev` 分支收敛该阶段代码，完成后再合并回 `master`。

结论：默认以 `master` 为发布分支推进，但保留在“大阶段”中临时启用 `dev` 分支的权利。

## 4. 实施总原则

- 优先复用现有 Worker 架构、D1 存储方式、前端工程结构。
- 优先迁移业务能力，不照搬 Node/Express/Docker/PM2 外壳。
- 新增功能以“客户端链接”作为核心实体，而不是沿用旧项目“一个账号一个订阅链接”的模型。
- 每个阶段完成后，代码必须保持结构清晰，可继续叠加下一阶段改造。
- 每个阶段都要保留可部署性，不允许留下明显的半成品路由或破坏主流程的死代码。
- 不做本地运行测试，但可以做静态检查、类型检查、代码审阅和结构验证。

## 5. 目标架构

### 保留

- `packages/frontend`：管理后台前端
- `packages/worker`：Worker API 与公开订阅入口
- `packages/shared`：共享类型与校验器
- Cloudflare GitHub 自动构建与部署链路

### 新增/重构

- `templates`：模板管理与版本管理
- `subscriptions`：订阅源管理与测试
- `client_profiles`：客户端链接管理
- `generation`：配置生成、诊断、缓存回退
- `settings`：区域关键字、清洗规则、生成参数

## 6. 分阶段计划

### 阶段 0：准备阶段

目标：

- 固化改造方案与节奏。
- 在项目根目录维护一份长期更新的计划文档。
- 明确后续每个阶段的交付边界。

范围：

- 建立本计划文档。
- 明确分支与发布策略。
- 明确“阶段完成后才推送部署”的工作方式。

完成标准：

- 计划文档已落地。
- 后续阶段有清晰的先后顺序。

---

### 阶段 1：移植 `proxyhub` 登录页和后台壳

目标：

- 用 `proxyhub` 的登录页和后台信息架构替换当前 `meta-manager` 的简化界面壳。

范围：

- 登录页布局与视觉风格迁移。
- 后台主框架迁移：侧边栏、顶部区域、工作区容器、通知区域。
- 前端导航结构重组，为后续模板、订阅源、客户端链接、生成测试等页面预留入口。

必须保留：

- 当前项目可正常作为单页应用加载。
- 当前模板管理入口先保留为现有唯一主功能页。

本阶段不做：

- 不引入订阅源逻辑。
- 不引入客户端链接逻辑。
- 不改造后端数据模型。

完成标准：

- 新登录页与后台框架已切换到 `proxyhub` 风格。
- 原有模板管理功能仍能在新壳内继续承载。

---

### 阶段 2：重构后台认证与访问控制

目标：

- 让新后台壳与当前 Worker 认证模型稳定衔接。

范围：

- 登录态读取与失效处理。
- 前端未登录拦截。
- 登录后页面初始化逻辑。
- 退出登录流程。

必须保留：

- 现有管理员密码登录模式。
- Worker 端 token 鉴权能力。

本阶段不做：

- 不引入多用户注册、审核、成员权限。

完成标准：

- 登录、退出、鉴权拦截在新壳下完整可用。

---

### 阶段 3：迁移并增强模板管理

目标：

- 保留 `singbox-meta-manager` 原有模板管理完整能力，并为后续生成链路做准备。

范围：

- 模板列表、详情、创建、编辑、删除、克隆。
- Monaco JSON 编辑器。
- 模板格式化、语法校验。
- 模板订阅链接复制。
- 为模板版本历史预留数据结构。

必须保留：

- 当前模板编辑的完整业务功能。
- 当前 API 的核心读写能力。

增强目标：

- 为后续模板版本管理补充可扩展字段或辅助结构。

完成标准：

- 模板管理能力不弱于当前仓库。

---

### 阶段 4：引入模板版本管理

目标：

- 吸收 `proxyhub` 的模板版本思想，补足模板演进能力。

范围：

- 模板版本表设计。
- 新版本保存。
- 历史版本查看。
- 回滚到指定版本。

必须保留：

- 当前模板主编辑体验不被破坏。

完成标准：

- 模板可以保留历史版本并恢复。

---

### 阶段 5：迁移订阅源管理

目标：

- 引入 `proxyhub` / `singbox-center` 的订阅源管理能力。

范围：

- 订阅源列表。
- 新增、编辑、删除。
- 启用/停用。
- 允许区域配置。
- 订阅源即时测试。
- 订阅 URL 安全校验与安全存储。

必须保留：

- 订阅测试、节点统计、警告信息。
- 原有订阅管理的业务语义。

完成标准：

- 订阅源已经是系统中的独立一类资源，可单独维护。

---

### 阶段 6：新增客户端链接管理

目标：

- 实现新项目最核心的新业务模型。

范围：

- 客户端链接列表。
- 新建、编辑、删除、启停。
- 复制订阅地址。
- 重置公开 token。
- 为每个客户端链接选择模板。
- 为每个客户端链接绑定多个订阅源。
- 支持绑定顺序和局部覆盖配置。

必须保留：

- 公开链接长期稳定可用，只有手动重置后才变化。

完成标准：

- 一个后台中可以维护多个客户端链接，且每个链接可选择不同模板和订阅集合。

---

### 阶段 7：迁移配置生成引擎

目标：

- 将 `singbox-center` / `proxyhub` 的核心生成能力迁入 Worker 架构。

范围：

- 根据客户端链接读取模板与订阅绑定。
- 并发抓取订阅。
- 节点解析与清洗。
- 区域匹配与分组。
- 模板注入与最终配置拼装。
- 生成失败时的错误传递。

必须保留：

- 节点清洗。
- 区域分组。
- 模板注入。
- 配置生成核心语义。

完成标准：

- 公开订阅接口不再只是返回模板，而是根据客户端链接动态生成结果。

---

### 阶段 8：迁移生成测试、运行记录和缓存回退

目标：

- 补足 `proxyhub` 中最重要的可观测能力。

范围：

- 测试生成页面。
- 步骤化诊断报告。
- 生成历史记录。
- 最近一次成功配置缓存。
- 生成失败时的缓存回退。

必须保留：

- 失败时可回退最近一次成功配置。
- 诊断报告可追踪每一步发生了什么。

完成标准：

- 系统不仅能生成，还能解释生成过程并在失败时兜底。

---

### 阶段 9：迁移系统设置

目标：

- 将生成参数从硬编码中抽离出来。

范围：

- 区域关键字字典。
- 节点屏蔽规则。
- 测速 URL 与容差参数。
- 生成缓存策略。

必须保留：

- 原有可配置参数能力不能丢失。

完成标准：

- 不改代码即可调整主要生成参数。

---

### 阶段 10：收尾与发布整理

目标：

- 对照旧项目检查迁移功能是否有缩减。

范围：

- 功能对照清单。
- 数据结构清理。
- 文档补充。
- 发布说明整理。

完成标准：

- 新项目形成完整闭环。
- 已迁移能力对照可追踪。

## 7. 每阶段工作流程

每个阶段统一按照以下步骤执行：

1. 明确该阶段边界和本次修改范围。
2. 只读取本阶段相关代码和数据结构。
3. 先改数据结构，再改 Worker API，再改前端页面。
4. 完成后补充或更新文档。
5. 不做本地运行测试。
6. 阶段收敛后再同步 GitHub，并触发 Worker 构建部署。
7. 由线上环境进行验证，再决定是否进入下一阶段。

## 8. 推送策略

### 默认策略

- 在单个阶段全部完成前，不推送。
- 阶段完成后统一推送 `master`。
- 由 Cloudflare Worker 自动构建部署。

### 备用策略

如果某个阶段明显超过一次连续改造可承受的规模，则采用：

- 本地继续该阶段开发。
- 进入真实 Git 仓库后切 `dev` 分支承接该阶段。
- 阶段收敛后再合并到 `master`。

## 9. 后续执行顺序

后续按以下顺序逐阶段推进：

1. 阶段 1：移植 `proxyhub` 登录页和后台壳
2. 阶段 2：重构后台认证与访问控制
3. 阶段 3：迁移并增强模板管理
4. 阶段 4：引入模板版本管理
5. 阶段 5：迁移订阅源管理
6. 阶段 6：新增客户端链接管理
7. 阶段 7：迁移配置生成引擎
8. 阶段 8：迁移生成测试、运行记录和缓存回退
9. 阶段 9：迁移系统设置
10. 阶段 10：收尾与发布整理

## 10. 下一步

分阶段改造已经完成。下一步不是本地构建，而是：

- 将源码同步到真实 Git 仓库。
- 只提交源码、配置和文档，不提交 `dist`、`.wrangler`、`node_modules`。
- 推送 GitHub 后由 Cloudflare Worker 构建部署。
- 按 `RELEASE_CHECKLIST.md` 进行线上验证。
